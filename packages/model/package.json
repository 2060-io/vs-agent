{
  "name": "@2060.io/service-agent-model",
  "main": "build/index",
  "types": "build/index",
  "version": "0.0.1",
  "files": [
    "build"
  ],
  "publishConfig": {
    "access": "public"
  },
  "license": "Apache-2.0",
  "scripts": {
    "build": "yarn run clean && yarn run compile",
    "clean": "rimraf -rf ./build",
    "compile": "tsc -p tsconfig.build.json",
    "prepublishOnly": "yarn run build",
    "test": "jest"
  },
  "dependencies": {
    "@2060.io/credo-ts-didcomm-mrtd": "^0.0.10",
    "@credo-ts/core": "^0.5.11",
    "class-transformer": "0.5.1",
    "class-validator": "0.14.1",
    "credo-ts-receipts": "^0.0.1-alpha.5",
    "iso-3166-1": "^2.1.1",
    "mrz": "^4.2.0"
  },
  "devDependencies": {
    "@types/node-tlv": "^1.5.4",
    "ts-node-dev": "^2.0.0"
  },
  "peerDependencies": {
    "@credo-ts/core": "^0.5.11"
  }
}
